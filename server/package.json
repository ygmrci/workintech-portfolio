{
  "name": "portfolio-api",
  "private": true,
  "type": "module",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch db.json"
  }
}
